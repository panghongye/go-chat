import withRouter from 'umi/withRouter'
import { observer } from 'mobx-react'
import React from 'react'
import store from './models_/index'

export function rootContainer(container: JSX.Element) {
	return container
}
